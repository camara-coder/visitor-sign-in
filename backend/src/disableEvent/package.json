{
    "name": "disable-event",
    "version": "1.0.0",
    "description": "Lambda function to disable an event",
    "main": "index.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "build": "mkdir -p dist && zip -r dist/disable-event.zip index.js"
    },
    "keywords": [
      "lambda",
      "event",
      "disable"
    ],
    "author": "",
    "license": "ISC"
  }